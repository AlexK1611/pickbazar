import styled from 'styled-components'

export const CartContainer = styled.div`
    position: fixed;
    z-index: 50;
    top: 0;
    right: 0;
    width: 400px;
    height: 100vh;
    box-shadow: 0px 21px 36px rgba(0, 0, 0, 0.16);
    background-color: #FFF;
`